name: test

# The committed guest fixtures (tests/wasm/*.wasm) make this job toolchain-free:
# no WASI SDK, no wasm32 target, no npm — just Rust + PHP dev headers.
on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: PHP ${{ matrix.php }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        php: ['8.4', '8.5']
    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          # NTS build; setup-php installs the matching dev headers (php-config).
        env:
          phpts: nts

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@1.96
        with:
          components: rustfmt

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: php-${{ matrix.php }}

      - name: Install build dependencies (clang/libclang for bindgen)
        run: sudo apt-get update && sudo apt-get install -y clang libclang-dev

      - name: Check formatting
        run: cargo fmt --check

      - name: Build
        run: cargo build

      - name: Rust unit tests
        run: cargo test --lib

      - name: PHP integration suites
        run: |
          EXT="$(pwd)/target/debug/libterrarium.so"
          php -d extension="$EXT" -r 'exit(class_exists("Terrarium\\Runtime") ? 0 : 1);' \
            || { echo "extension failed to load"; exit 1; }
          fail=0
          for t in tests/php/[0-9]*.php; do
            echo "== $t =="
            php -d extension="$EXT" "$t" || fail=1
          done
          exit $fail

      - name: Examples
        run: |
          EXT="$(pwd)/target/debug/libterrarium.so"
          for ex in examples/*.php; do
            echo "== $ex =="
            php -d extension="$EXT" "$ex" > /dev/null || exit 1
          done

  # Audit the committed Cargo.lock against the RustSec advisory database.
  audit:
    name: cargo audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.96
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit
      - name: Audit dependencies
        run: cargo audit
